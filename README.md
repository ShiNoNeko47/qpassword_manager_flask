# qpassword_manager_flask
### Flask api for qpassword_manager

It uses redis with redisjson as the database, so to use it you would have to set those up first.

To start the server just run app.py.

## TODO
 - mail verification
 - code reformatting
